ignore-scripts=true
# openapi-typescript@7 peer-requires typescript@^5.x; repo is on typescript~6.0.3.
# The generator is a build-time dev tool — its TypeScript peer does not affect the
# compiled output. legacy-peer-deps lets npm ci resolve it without ERESOLVE.
legacy-peer-deps=true
