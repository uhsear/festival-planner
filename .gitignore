node_modules

# Environment files
.env
.env.local
.env.production
.env.*.local

# Secrets / credentials
*.pem
*.key
credentials*.json
# Google Play / GCP service-account keys (never commit)
*service-account*.json
*-service-account.json
festie-*.json
play-store-key*.json
google-play*.json

# Python tooling caches (packages/mobile/store/play)
__pycache__/
*.pyc

# Build outputs
coverage/
output/
lhci-reports/
.turbo/
dist/
dist.disabled/
packages/web/dist/

# Playwright debug scripts
pw-*.js

# IDE / OS
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Local editor/tool settings
.claude/
.agents/
.mcp.json
skills-lock.json
mobile-loop-capture.js

# Code review graph DB
.code-review-graph/

# Playwright MCP and test artifacts
.playwright-mcp/
*.png
# but DO track the mobile app icons/splash (required by EAS prebuild)
!packages/mobile/assets/*.png
# and DO track the Play Store listing assets (icon, feature graphic, screenshots)
!packages/mobile/store/play/images/**/*.png

# Session logs and loop captures
docs/session-logs/
mobile-loop-*/

*storybook.log
storybook-static

# Orphaned root pnpm lock — the real workspace is packages/ (uses packages/pnpm-lock.yaml). Root installs via npm.
/pnpm-lock.yaml

# Playwright auth storage-state (contains live session cookies — never commit)
tests/e2e/.auth/

# TypeScript incremental build cache (changes every build)
*.tsbuildinfo

# Local per-session state doc (operational; not for the public repo).
# NOTE: do NOT ignore /context.md — that case-collides with the tracked public CONTEXT.md on Windows.
/SESSION.md
