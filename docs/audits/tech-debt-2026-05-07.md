# Tech Debt Audit -- 2026-05-07 (v2)

**Auditor:** Claude Code (9 parallel agents + main-thread coordination)
**Scope:** Full codebase -- dependencies, complexity, tests, database, frontend, security, config, operations, accessibility
**Branch:** `master` @ `ee9ad27`
**Previous audit:** 2026-05-01 (score 5.5 initial, 7.0 post-remediation)

---

## Tech Debt Score: 5.5/10

**Rubric:** 10 = zero HIGHs, current-minor deps, >= 80% coverage, all a11y AA

| Deduction | Reason |
|-----------|--------|
| -2.0 | 5 CRITICAL items (Zustand double-sub, hard-delete no txn, 600+ line factories, ICS duplication, 26px touch target) |
| -1.5 | 15+ HIGH items across complexity, database, testing, operations, a11y |
| -0.5 | Frontend test coverage: 0% route tests, 61% components untested, 0% shared hooks |
| -0.5 | a11y AA failures: touch targets, missing ARIA, no route-change focus management |

---

## Top 5 by Impact/Effort Ratio

| # | Item | Cat | Sev | Effort | Why |
|---|------|-----|-----|--------|-----|
| 1 | CORS missing `PATCH` method | Security | MEDIUM | XS | Functional bug -- frontend PATCH requests fail cross-origin. One-line fix in `lib/middleware.js:96`. |
| 2 | Grid share button 26px touch target | a11y | CRITICAL | XS | Every mobile user. CSS change: 26px to 44px in `pages.css:290-291`. |
| 3 | `useFestivalStore` facade double-subscription | Frontend | CRITICAL | M | Every component using the facade re-renders on ANY store change. 14 calls in AppShell alone. |
| 4 | Festival hard-delete without transaction | Database | CRITICAL | S | 9 sequential DELETEs; partial failure = orphaned data. Wrap in `withTransaction()`. |
| 5 | Toast close/undo buttons < 44px | a11y | HIGH | XS | Time-critical undo action on ~24px target. Add two Tailwind classes. |

---

## Blast-Radius Map (CRITICAL Items)

```
useFestivalStore double-subscription (CRITICAL)
  └─ AppShell.tsx (14 calls) ─── every page render
  └─ SubHeader.tsx (11 calls) ── every page with sub-header
  └─ grid.tsx, timeline.tsx, picks.tsx, crew.tsx ── all main views
  └─ Impact: unnecessary re-renders across entire app on any store field change

Festival hard-delete without transaction (CRITICAL)
  └─ DELETE festival_profile_picks → festival_profile_notes → festival_profiles
  └─ DELETE festival_sets → festival_stages → festival_days
  └─ DELETE crews → notification_counts → festivals
  └─ Impact: any failure mid-cascade leaves orphaned rows, breaks FK integrity

ICS calendar generation duplication (CRITICAL)
  └─ routes/export.js L240-298 (schedule export)
  └─ routes/calendar-sync.js L77-142 (calendar sync)
  └─ Impact: bug fix in one copy won't propagate; divergent ICS output

Factory functions > 600 lines (CRITICAL)
  └─ createCrewRoutes (638 lines) ── all crew endpoints
  └─ AppShell component (485 lines) ── root layout
  └─ createExportRoutes (477 lines) ── all export endpoints
  └─ Impact: unmaintainable; high risk of regression when modifying

Grid share button 26px (CRITICAL)
  └─ pages.css:290-291, rendered by routes/pages.js
  └─ Impact: mobile users cannot reliably tap share button on grid export
```

---

## Category 1: Dependencies

**Overall: GOOD.** Nearly all packages current. 0 critical/high vulnerabilities.

| Item | Severity | Effort | Impact | Recommendation |
|------|----------|--------|--------|----------------|
| `firebase-admin` 13.8.0 -> 13.9.0 (pinned with `~`) | MEDIUM | XS | Patch with transitive vuln fixes | Bump to `~13.9.0` |
| ESLint 10 early adoption (npm `latest` still 9.x) | MEDIUM | S | Plugin ecosystem may lag | Pin exact versions in CI; monitor compatibility |
| `@tootallnate/once` < 3.0.1 via firebase-admin (8 instances) | LOW | S | Transitive; no direct exposure | Update firebase-admin; wait for upstream fix |
| `tmp` <= 0.2.3 via `@lhci/cli` (dev-only) | LOW | XS | Dev dependency; not shipped | Monitor for `@lhci/cli` update |
| `@use-gesture/react` last publish Mar 2024 (14+ months) | LOW | M | Works fine; maintenance mode | Monitor; consider alternatives if bugs arise |
| Mixed `~`/`^` ranges: `helmet ~8.1.0`, `firebase-admin ~13.8.0` | LOW | XS | Tilde prevents auto-minor | Switch `helmet` and `firebase-admin` to `^`; keep `pg`/`socket.io` on `~` |
| `zod` range mismatch: root `^4.3.6` vs web `^4.4.3` | LOW | XS | Cosmetic; both resolve to 4.4.3 | Align root to `^4.4.3` |
| No deprecated Node.js APIs found | -- | -- | -- | Clean |

**Severity counts:** 0 CRITICAL, 0 HIGH, 2 MEDIUM, 5 LOW

---

## Category 2: Complexity

### 2.1 Large Files (approaching 700-line limit)

| File | Lines | Severity | Effort | Recommendation |
|------|-------|----------|--------|----------------|
| `lib/schemas.js` | 676 | MEDIUM | L | Split into domain-specific schema modules |
| `routes/crews.js` | 641 | MEDIUM | M | Extract sub-route groups (invites, polls, meeting points) |
| `routes/auth.js` | 528 | MEDIUM | M | Split OAuth/password-reset flows |
| `AppShell.tsx` | 527 | MEDIUM | M | Extract SubHeader, BottomNav into composed children |
| `routes/export.js` | 485 | MEDIUM | M | Extract ICS/HTML/PDF builders to helpers |

### 2.2 Oversized Functions (top 10 of 135 violating <50 line limit)

| Function | Lines | Severity | Recommendation |
|----------|-------|----------|----------------|
| `createCrewRoutes` (crews.js) | 638 | CRITICAL | Break into sub-routers |
| `AppShell` component | 485 | CRITICAL | Extract sub-components |
| `createExportRoutes` (export.js) | 477 | CRITICAL | Extract builders to lib/ |
| `createAuthRoutes` (auth.js) | 460 | HIGH | Extract sub-modules |
| `AccountPage` component | 454 | HIGH | Extract sections |
| `createSendService` (send.js) | 436 | HIGH | Deduplicate device-token validation |
| `createAdminStatusRoutes` | 419 | HIGH | Extract inline HTML template |
| `UserMenu` component | 352 | HIGH | Extract menu sections |
| `createAccountRoutes` | 332 | HIGH | Extract helpers |
| `createAppContext` (index.js) | 328 | HIGH | Split into sub-contexts |

### 2.3 Duplicate Logic

| Item | Severity | Effort | Impact | Recommendation |
|------|----------|--------|--------|----------------|
| ICS calendar generation: `export.js` vs `calendar-sync.js` (~60 lines each) | CRITICAL | S | HIGH | Extract to `lib/helpers/ics-builder.js` |
| `INSERT INTO festival_sets` in stores + routes (4 occurrences) | HIGH | S | HIGH | Centralize in `stores.festivals.insertSet()` |
| Token cleanup queries across 3 files (9 occurrences) | HIGH | S | MEDIUM | Extract to `stores.sessions.cleanupExpiredTokens()` |
| Catch-log-sendError pattern (43+ identical instances in routes/) | HIGH | M | HIGH | Create `asyncHandler(fn)` wrapper |
| `escapeHtml` defined in `reset-pages.js` and `sanitize.js` | MEDIUM | XS | MEDIUM | Use canonical from `sanitize.js` |
| Device token validation (3 locations in send.js) | MEDIUM | XS | MEDIUM | Extract `isValidDeviceToken()` helper |

### 2.4 Dead Code

| Item | Severity | Effort | Recommendation |
|------|----------|--------|----------------|
| `lib/file-storage.js` -- never imported | HIGH | XS | Delete |
| `lib/error-codes.js` -- never imported | HIGH | XS | Delete |
| `CrewOverlapHeat.tsx` -- never imported | MEDIUM | XS | Delete |
| `FestivalSelector.tsx` -- never imported | MEDIUM | XS | Delete |
| `SubHeader.tsx` -- never imported (logic duplicated in AppShell) | MEDIUM | XS | Delete or use to replace inline logic |
| `useCrewJoin.ts`, `useOfflineSnapshot.ts`, `useServiceWorker.ts`, `useReducedMotion.ts` -- never imported | MEDIUM | XS | Delete |
| `schemas.adminLogin`, `schemas.expenseSettle` -- exported but unused | LOW | XS | Delete |

### 2.5 Deep Nesting (>4 levels, top 5)

| File | Max Depth | Severity | Recommendation |
|------|-----------|----------|----------------|
| `TimelineGrid.tsx` | 17 | HIGH | Extract row/cell sub-components |
| `timeline.tsx` | 16 | HIGH | Extract TBASection |
| `PollsTab.tsx` | 15 | HIGH | Extract PollItem, VoteBar |
| `compare.tsx` | 15 | HIGH | Extract column/row renderers |
| `crew.tsx` | 14 | MEDIUM | Push more JSX into tab components |

**Severity counts:** 3 CRITICAL, 10 HIGH, 14 MEDIUM, 3 LOW

---

## Category 3: Test Gaps

### 3.1 Coverage Summary

| Metric | Backend | Frontend | Shared |
|--------|---------|----------|--------|
| Test files | 69 | 27 | 11 |
| Test cases | 2,507 | 324 | 244 |
| Statement coverage | 87.46% | unmeasured | unmeasured |
| Branch coverage | 79.46% | unmeasured | unmeasured |
| Function coverage | 84.86% | unmeasured | unmeasured |

### 3.2 Low-Coverage Backend Files

| File | Stmts | Severity | Effort | Recommendation |
|------|-------|----------|--------|----------------|
| `lib/presence.js` | 29% | CRITICAL | M | Core real-time presence; test join/leave/heartbeat |
| `lib/avatar-pool.js` | 34% | HIGH | S | Test pool lifecycle, resize paths |
| `lib/emitter.js` | 36% | HIGH | S | Test on/off/emit/once, error propagation |
| `routes/pages.js` | 41% | HIGH | S | Test SSR rendering, Open Graph meta |
| `lib/export-worker.js` | 44% | MEDIUM | S | Test worker message handling |
| `lib/crypto-auth.js` | 51% | HIGH | S | Security-critical; test hash/verify |
| `lib/middleware.js` | 55% | HIGH | M | Test CSRF, session validation |
| `server.js` | 62% | HIGH | L | Test startup, shutdown, middleware ordering |

### 3.3 Frontend Test Gaps

| Area | Tested | Total | Gap | Severity |
|------|--------|-------|-----|----------|
| Route pages | 0 | 13 | 100% | CRITICAL |
| Components | 25 | 64 | 61% | HIGH |
| Hooks | 2 | 13 | 85% | HIGH |
| Shared hooks | 0 | 6 | 100% | HIGH |
| Shared stores | 7 | 8 | 12% | LOW |
| Frontend coverage instrumentation | -- | -- | missing | HIGH |

### 3.4 Test Quality Issues

| Item | Severity | Effort | Impact | Recommendation |
|------|----------|--------|--------|----------------|
| 24 long setTimeout instances (>= 2s) across 4 test files | HIGH | M | CI flakiness | Replace with event-driven waits |
| 16s setTimeout in `redis-metrics-retry.test.js` | HIGH | M | CI slowdown | Mock timers |
| ~151 test cases with no visible assertions | MEDIUM | M | May pass vacuously | Audit; many use supertest `.expect()` chains (valid) |
| 5 Socket.IO tests using timeout-as-assertion pattern | MEDIUM | S | Semantically wrong | Assert on specific events |
| 15 E2E test cases total (3 spec files) | MEDIUM | L | Limited browser coverage | Add crew, offline, admin E2E |

**Severity counts:** 1 CRITICAL, 9 HIGH, 5 MEDIUM, 1 LOW

---

## Category 4: Database

### 4.1 Query Pattern Issues

| Item | Severity | Effort | Impact | Recommendation |
|------|----------|--------|--------|----------------|
| Festival hard-delete: 9 sequential DELETEs without transaction | CRITICAL | S | Data integrity on partial failure | Wrap in `withTransaction()` |
| `festivals.getById()` calls `readAll()` then `.find()` | HIGH | M | Loads ALL festivals for single lookup | Add direct `SELECT ... WHERE id = $1` |
| `share.js` calls `users.readAll()` + `profiles.readAll()` | HIGH | S | Loads entire tables for single share URL | Use targeted `getByUsername()` + `getByFestival()` |
| `crews.js` calls `festivals.readAll().find()` to verify existence | MEDIUM | XS | Loads all festivals to check one | Use `SELECT 1 ... WHERE id = $1` |
| `auth.js` calls `getUsers()` to find by username / check count | MEDIUM | S | Login loads ALL users | Use `getByUsername()` + `count()` |
| N+1 profile updates in auth.js registration loop | MEDIUM | S | One query per orphan profile | Single `UPDATE ... WHERE` batch |
| `profiles.readAll()` has `LIMIT 10000` default | MEDIUM | S | Expensive with correlated subqueries | Add cursor pagination |

### 4.2 Schema Issues

| Item | Severity | Effort | Impact | Recommendation |
|------|----------|--------|--------|----------------|
| `festivals.name`, `festival_sets.day_index`, `festival_sets.artist` allow NULL | MEDIUM | XS | Data integrity gaps | Add `NOT NULL DEFAULT ''` |
| `crew_polls.created_by`, `crew_poll_votes.user_id`, `crew_meeting_points.created_by` -- no explicit ON DELETE | MEDIUM | S | Implicit NO ACTION; works but inconsistent with 031 RESTRICT pattern | Add explicit `ON DELETE RESTRICT` |
| `notification_preferences`, `notification_topic_subs` use INTEGER (0/1) not BOOLEAN | LOW | S | Inconsistent with other boolean columns | Migrate to BOOLEAN |
| `crews.home_base_*` columns legacy (superseded by `crew_meeting_points`) | LOW | M | Dead columns still written | Drop after confirming reads moved |
| `festival_profiles.picks_json/notes_json` dual-write to JSONB + normalized tables | MEDIUM | M | Doubles write amplification | Phase out JSONB writes |
| `users.purgeDeleted()` exists but is never called | HIGH | S | Dead code; no automatic purging of soft-deleted users | Wire into retention_cleanup() |
| No scheduled cleanup of soft-deleted festivals | MEDIUM | S | Old soft-deleted festivals accumulate | Add to retention_cleanup() |

### 4.3 Migration Quality

| Item | Severity | Effort | Impact | Recommendation |
|------|----------|--------|--------|----------------|
| No down migrations for any of 32 files | MEDIUM | L | No automated rollback | Add rollback for last 5 migrations |
| Migration numbering collision (two files with version 019/028) | MEDIUM | XS | `schema_migrations` PK conflict | Renumber colliding files |

**Severity counts:** 1 CRITICAL, 3 HIGH, 11 MEDIUM, 3 LOW

---

## Category 5: Config

| Item | Severity | Effort | Impact | Recommendation |
|------|----------|--------|--------|----------------|
| DB pool: `DB_POOL_SIZE` (15) vs `PG_POOL_MAX` (20) overlap | MEDIUM | XS | Pool uses 15, not the expected 20 | Remove `DB_POOL_SIZE`; use `PG_POOL_MAX` consistently |
| `.env.example` missing 14 env vars that config.js reads | MEDIUM | S | Onboarding friction | Update `.env.example` with all vars |
| Rate limits per-IP only; no per-user limiting | MEDIUM | M | Shared-IP users penalized | Add user-scoped rate limiting tier |
| Idempotency/error-dedup constants hardcoded in middleware.js/server.js | LOW | XS | Not tunable per environment | Move to config.js DEFAULTS |
| `festie.us` hardcoded in export.js SVG branding | LOW | XS | Not portable | Derive from `config.PUBLIC_ORIGIN` |
| Memory threshold `384` hardcoded in shutdown.js | LOW | XS | Duplicates PM2 value | Use config or derive from `MAX_HEAP_BYTES` |
| Stale `ADMIN_USER`/`ADMIN_PASSWORD` in `.env.example` | LOW | XS | Misleading documentation | Remove |

**Severity counts:** 0 CRITICAL, 0 HIGH, 3 MEDIUM, 4 LOW

---

## Category 6: Frontend

### 6.1 Component Size & Store Patterns

| Item | Severity | Effort | Impact | Recommendation |
|------|----------|--------|--------|----------------|
| `useFestivalStore` facade causes double-subscription | CRITICAL | M | Every component re-renders on ANY store change | Rewrite facade to use direct store imports or `useSyncExternalStoreWithSelector` |
| AppShell 527 lines + 14 facade selector calls | HIGH | L | Root layout; performance + maintainability | Split component; use direct store imports |
| `account.tsx` (466 lines) | MEDIUM | M | Extract ProfileSection, NotificationSection, DangerZone |  |
| `timeline.tsx` (441 lines) | MEDIUM | M | Extract filtering logic to hook; split day-group rendering |  |
| `useOfflineQueue.ts` (377 lines) | MEDIUM | M | Extract IndexedDB logic to standalone service |  |

### 6.2 Bundle & Theme

| Item | Severity | Effort | Impact | Recommendation |
|------|----------|--------|--------|----------------|
| `ui-motion` chunk 150 KB (49 KB gzip) | MEDIUM | M | Heaviest app dependency | Audit usage; consider `motion/mini` or CSS transitions |
| 55+ arbitrary Tailwind values (`text-[13px]`, `min-w-[44px]`, etc.) | MEDIUM | M | Should map to theme tokens | Define `--touch-min`, extend font-size scale |
| Hardcoded `var()` references bypassing Tailwind theme in login/register/picks | MEDIUM | XS | Use existing `text-accent-aqua`, `text-text-secondary` classes |  |
| 2 dead components: `CrewOverlapHeat.tsx`, `FestivalSelector.tsx` | MEDIUM | XS | Never imported | Delete |

### 6.3 Accessibility (WCAG AA -- Static Analysis)

| Item | Severity | Effort | Impact | Recommendation |
|------|----------|--------|--------|----------------|
| Grid share button 26x26px | CRITICAL | XS | Below 44px AA minimum | Change to 44px in pages.css |
| Toast close button ~24px | HIGH | XS | Below 44px | Add `min-h-11 min-w-11` |
| Toast undo button no min dimensions | HIGH | XS | Time-critical action | Add `min-h-11 min-w-11` |
| No focus management on route changes | HIGH | M | Screen readers get no page-change announcement | Add route-change focus reset to `#main-content` |
| StageBadge `role="img"` on visible text span | HIGH | XS | Hides text from accessibility tree | Remove `role="img"` |
| Spotify toggle missing `aria-expanded` | HIGH | XS | Screen readers can't determine state | Add `aria-expanded={visible}` |
| Auth tabs missing `role="tablist"` / `role="tab"` / `aria-selected` | HIGH | S | Tab navigation invisible to screen readers | Add ARIA tab pattern |
| StageBadge inactive chip -- light colors may fail 4.5:1 contrast | MEDIUM | S | Depends on stage color value | Add contrast check for faded variant |
| GuestTeaser uses `<h1>` (duplicate) | MEDIUM | XS | Violates heading hierarchy | Change to `<h2>` |
| MeetingPointsTab type selector has no `role="radiogroup"` | MEDIUM | XS | Behaves as radio group without semantics | Add ARIA radio pattern |
| HomeBaseCard / MeetingPointsTab placeholder-only labels | MEDIUM | XS | No persistent accessible name | Add `aria-label` to inputs |
| DetailConflictWarning "Switch to this" -- ambiguous label | MEDIUM | XS | Repeated without identifying the artist | Add `aria-label={`Switch to ${artistName}`}` |
| DetailCrewSection uses `<div>` instead of `<ul>/<li>` | MEDIUM | XS | Screen readers can't convey list structure | Use semantic list elements |
| CrewSelector dropdown lacks focus trap | MEDIUM | S | Keyboard users tab out of dropdown | Implement focus trap |
| Font fallback `@font-face` missing `font-display: swap` | MEDIUM | XS | Minor CLS risk | Add `font-display: swap` |
| RatingButtons compact mode 36px | MEDIUM | XS | Below 44px target | Increase to w-11 h-11 |
| IconButton `sm` variant 36px | MEDIUM | XS | Below 44px target | Change to 44px |
| Admin tables missing `<caption>` | LOW | XS | Screen reader usability | Add captions |
| Console.error/warn -- 18 instances in catch blocks | LOW | S | Appropriate but could route to structured logger | Consider Sentry integration |

**Severity counts:** 2 CRITICAL, 7 HIGH, 16 MEDIUM, 4 LOW

---

## Category 7: Security

**Security fundamentals are strong:** scrypt hashing, SHA-256 tokens, parameterized SQL, Zod validation, CSRF origin enforcement, multi-tier rate limiting, PII log redaction.

| Item | Severity | Effort | Impact | Recommendation |
|------|----------|--------|--------|----------------|
| CORS `Access-Control-Allow-Methods` missing `PATCH` | MEDIUM | XS | Frontend PATCH requests fail cross-origin | Add `PATCH` to `lib/middleware.js:96` |
| Ratings routes missing `validateParams` (5 endpoints) | MEDIUM | S | Raw `req.params` without Zod validation | Add `validateParams()` middleware |
| `actorId` falls back to string `'admin'` in admin-users.js | MEDIUM | XS | Audit trail loses traceability | Throw/401 instead of defaulting |
| `/internal/metrics-json` uses IP-only auth | MEDIUM | S | Spoofable behind reverse proxy with `trust proxy` | Add `adminAuth` or static API key |
| CSP uses `report-uri` (deprecated) without `report-to` | MEDIUM | S | Violations may go undetected in newer browsers | Add `report-to` directive |
| No CSP violation alerting | MEDIUM | M | Reports received but not aggregated/alerted | Wire to audit log or monitoring |
| Password reset per-email limit is in-memory only in cluster mode | MEDIUM | M | Each worker has its own counter | Use Redis-backed limiter |
| Socket.IO `allowRequest` accepts any valid hex without session check | MEDIUM | S | Unauthenticated connections consume slots for 10s | Validate session token in `allowRequest` |
| `style-src 'unsafe-inline'` in CSP | MEDIUM | XL | Documented tradeoff for React/motion runtime styles | Revisit when motion supports nonce-based styles |
| Self-demotion guard only on role revocation, not user deletion | LOW | S | Admin could delete own account | Add self-deletion guard |
| `paginationQuery.parse()` in ratings returns 500 instead of 400 | LOW | XS | Malformed pagination params get wrong status code | Use `validateQuery` middleware |

**Severity counts:** 0 CRITICAL, 0 HIGH, 9 MEDIUM, 2 LOW

**Positive observations:** No SQL injection vectors. Timing-safe auth. Progressive account lockout with jitter. Refresh token rotation with revocation detection. Comprehensive log redaction (15+ patterns). Defense-in-depth CSRF. Max 5 concurrent sessions with LRU eviction. Production startup validation.

---

## Category 8: Operations

| Item | Severity | Effort | Impact | Recommendation |
|------|----------|--------|--------|----------------|
| No backup restore test | HIGH | M | Backups that can't restore are worthless | Create `backup-verify-restore.sh`; run monthly |
| Log rotation relies on unverified `pm2-logrotate` | HIGH | S | If not installed, logs grow unbounded | Add startup verification step |
| No database migration runner on startup/deploy | MEDIUM | S | Migrations must be run manually | Add pre-start migration step |
| Two redundant backup scripts (`backup-pg.sh` vs `backup-db.sh`) | MEDIUM | S | Different formats, encryption, retention | Consolidate into one |
| Backup verification disabled by default (`BACKUP_VERIFY=0`) | MEDIUM | XS | Silent corruption undetected | Default to `BACKUP_VERIFY=1` |
| Off-site backup silently exits 0 if `OFFSITE_TARGET` unset | MEDIUM | XS | No warning of local-only backup risk | Log a WARNING |
| Background cleanup tasks run at fixed intervals, no jitter in 4-worker cluster | MEDIUM | S | Simultaneous DB cleanup load spikes | Add startup jitter or leader-election |
| No centralized alerting integration in the app itself | MEDIUM | M | Alerting depends entirely on external cron scripts | Add `ALERT_WEBHOOK_URL` to config; wire memory warnings |
| Docker health check uses hardcoded port 4000 | MEDIUM | XS | Breaks if `PORT` env var overridden | Derive from `process.env.PORT` |
| Docker installs `libvips-dev` instead of `libvips42` in production stage | LOW | XS | Unnecessary image size + attack surface | Use runtime-only package |
| Two redundant health check scripts (bash + Node) | LOW | S | Maintenance surface | Deprecate one |
| Prometheus metrics require manual scrape config | MEDIUM | S | No documentation or example provided | Add `prometheus.yml` example |
| Cron setup is manual (`setup-crons.sh` only prints instructions) | LOW | S | Two sources of truth for cron config | Add `--install` flag |
| `recap-script.js` hardcodes `festie.us` | LOW | XS | Not portable | Read from `PUBLIC_ORIGIN` |

**Severity counts:** 0 CRITICAL, 2 HIGH, 8 MEDIUM, 4 LOW

---

## Aggregate Severity Counts

| Category | CRITICAL | HIGH | MEDIUM | LOW | Total |
|----------|----------|------|--------|-----|-------|
| 1. Dependencies | 0 | 0 | 2 | 5 | 7 |
| 2. Complexity | 3 | 10 | 14 | 3 | 30 |
| 3. Test Gaps | 1 | 9 | 5 | 1 | 16 |
| 4. Database | 1 | 3 | 11 | 3 | 18 |
| 5. Config | 0 | 0 | 3 | 4 | 7 |
| 6. Frontend | 2 | 7 | 16 | 4 | 29 |
| 7. Security | 0 | 0 | 9 | 2 | 11 |
| 8. Operations | 0 | 2 | 8 | 4 | 14 |
| **Total** | **7** | **31** | **68** | **26** | **132** |

---

## Appendix: Schema Inventory

**29 active tables, 62 indexes, 38 foreign keys, 7 CHECK constraints, 1 view, 1 function, 3 extensions.**

### Tables

| Table | PK | Soft-Delete | FK Count | Notes |
|-------|-----|-------------|----------|-------|
| schema_migrations | version (INT) | -- | 0 | Migration tracking |
| users | id (TEXT) | deleted_at | 0 | CITEXT username, scrypt hash |
| user_sessions | token (TEXT) | -- | 1 | TIMESTAMPTZ (migrated from BIGINT in 036) |
| festivals | id (TEXT) | deleted_at | 0 | Geo columns added in 022 |
| festival_stages | (festival_id, id) | -- | 1 | Composite PK |
| festival_days | (festival_id, day_index) | -- | 1 | Composite PK |
| festival_sets | id (TEXT) | -- | 1 (composite) | Time CHECK constraints in 034 |
| festival_profiles | id (TEXT) | deleted_at | 2 | JSONB + normalized dual-write |
| festival_profile_picks | (profile_id, set_id) | -- | 2 | Normalized from picks_json |
| festival_profile_notes | (profile_id, set_id) | -- | 2 | Normalized from notes_json |
| crews | id (TEXT) | -- | 2 | No soft-delete; hard-delete only |
| crew_members | (crew_id, user_id) | -- | 2 | role CHECK: owner/member |
| crew_meeting_points | id (TEXT) | active=FALSE | 2 | Replaced home_base columns |
| crew_polls | id (TEXT) | -- | 2 | No ON DELETE on created_by FK |
| crew_poll_votes | (poll_id, user_id) | -- | 2 | No ON DELETE on user_id FK |
| crew_expenses | id (TEXT) | -- | 2 | amount > 0 CHECK |
| crew_activity | id (TEXT) | -- | 2 | Append-only feed |
| device_tokens | id (TEXT) | -- | 1 | 90-day TTL |
| notification_preferences | user_id (TEXT) | -- | 1 | INTEGER booleans (should be BOOLEAN) |
| notification_log | id (TEXT) | -- | 1 | Audit trail |
| notification_counts | (user_id, festival_id) | -- | 2 | Unread counters |
| notification_topic_subs | (user_id, festival_id, topic) | -- | 2 | INTEGER boolean |
| audit_log | id (TEXT) | -- | 0 | No FKs; 1-year retention |
| refresh_tokens | token (TEXT) | revoked | 1 | 90-day TTL |
| login_failures | user_id (TEXT) | -- | 1 | Lockout tracking |
| metrics_rollups | id (SERIAL) | -- | 0 | 90-day retention |
| roles | id (SERIAL) | -- | 0 | Seeded: user, moderator, admin |
| user_roles | (user_id, role_id) | -- | 2 | RBAC junction |
| password_reset_tokens | id (SERIAL) | -- | 1 | Unique token_hash |
| email_verification_tokens | id (SERIAL) | -- | 1 | Unique token_hash |
| set_ratings | id (UUID) | -- | 2 | Only native UUID PK in schema |
| calendar_tokens | id (TEXT) | -- | 2 | ICS sync |
| install_events | id (BIGSERIAL) | -- | 0 | PWA analytics |

### Schema Concerns

1. **Tables without `updated_at`**: crew_polls (closed can change), login_failures (mutates), crew_expenses, crew_activity, notification_log, install_events, metrics_rollups
2. **Missing NOT NULL**: festivals.name, festival_stages.name, festival_sets.day_index, festival_sets.artist, festival_days.label/date, crews.name
3. **Legacy columns**: crews.home_base_* (superseded by crew_meeting_points), user_sessions.username (denormalized)
4. **Dual-write**: festival_profiles.picks_json/notes_json alongside normalized tables
5. **TEXT PKs vs UUID**: Only set_ratings uses native UUID; rest use TEXT (nanoid)
6. **All BIGINT timestamps resolved**: Migrated to TIMESTAMPTZ in 015, 034, 036

### Dropped Tables (migration 013)

festival_profile_reminders, festival_messages, message_sequences, admin_sessions

### Extensions

citext (004), pg_stat_statements (029), pg_cron (034 -- optional)

---

*Report generated 2026-05-07 by Claude Code with 9 parallel analysis agents.*
*Previous audit: 2026-05-01 (score 5.5 -> 7.0 post-remediation). Delta: +89 new items cataloged; 43 previously fixed items verified.*
