import { Meta } from '@storybook/addon-docs/blocks';
import { colors } from '@festie/shared/tokens';

<Meta title="Tokens/Colors" />

# Color Tokens

All colors are defined in `packages/shared/src/tokens/colors.ts` and mirrored as CSS custom properties in `theme.css`.

export function Swatch({ color, name, contrastNote }) {
  const isTransparent = color.includes('rgba');
  return (
    <div style={{ display: 'flex', alignItems: 'center', gap: 12, marginBottom: 8 }}>
      <div
        style={{
          width: 48,
          height: 48,
          borderRadius: 8,
          backgroundColor: color,
          border: '1px solid rgba(255,255,255,0.1)',
          flexShrink: 0,
        }}
      />
      <div style={{ fontFamily: 'monospace', fontSize: 13 }}>
        <div style={{ color: '#eaeaf2', fontWeight: 600 }}>{name}</div>
        <div style={{ color: '#9999bb' }}>{color}</div>
        {contrastNote && (
          <div style={{ color: '#ffb020', fontSize: 11, marginTop: 2 }}>{contrastNote}</div>
        )}
      </div>
    </div>
  );
}

export function SwatchGroup({ title, tokens, contrastNotes }) {
  return (
    <div style={{ marginBottom: 32 }}>
      <h3 style={{ color: '#eaeaf2', marginBottom: 12, borderBottom: '1px solid rgba(255,255,255,0.06)', paddingBottom: 8 }}>{title}</h3>
      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(280px, 1fr))', gap: 4 }}>
        {Object.entries(tokens).map(([key, value]) => (
          <Swatch
            key={key}
            name={key}
            color={value}
            contrastNote={contrastNotes?.[key]}
          />
        ))}
      </div>
    </div>
  );
}

---

## Background Colors

<SwatchGroup
  title="bg"
  tokens={colors.bg}
/>

---

## Text Colors

<SwatchGroup
  title="text"
  tokens={colors.text}
  contrastNotes={{
    primary: 'AA on bg.primary (#080810) -- ratio ~16:1',
    secondary: 'AA on bg.primary -- ratio ~5.5:1',
    muted: 'AA on bg.primary -- ratio ~4.6:1',
    onAccent: 'Use on accent backgrounds only',
    onLightAccent: 'Use on light accent backgrounds only',
    onDark: 'Use on light surfaces only',
    danger: 'AA on bg.primary -- ratio ~5.2:1',
    disabled: 'Below AA on bg.primary -- decorative only',
    placeholder: 'Below AA -- decorative placeholder text',
  }}
/>

---

## Accent Colors

<SwatchGroup
  title="accent"
  tokens={colors.accent}
  contrastNotes={{
    coral: 'AA-large on bg.primary -- ratio ~4.1:1',
    aqua: 'AAA on bg.primary -- ratio ~10.8:1',
    amber: 'AAA on bg.primary -- ratio ~9.3:1',
    green: 'AAA on bg.primary -- ratio ~12.5:1',
    blue: 'AA on bg.primary -- ratio ~4.7:1',
  }}
/>

---

## Border Colors

<SwatchGroup title="border" tokens={colors.border} />

---

## Priority Colors

These map to the pick priority levels in the festival schedule.

<SwatchGroup
  title="priority"
  tokens={colors.priority}
  contrastNotes={{
    must: 'Same as accent.coral',
    want: 'Same as accent.aqua',
    maybe: 'Same as accent.amber',
  }}
/>

---

## Glass (Glassmorphism)

<SwatchGroup title="glass" tokens={colors.glass} />

---

## Stage Colors

<SwatchGroup
  title="stage"
  tokens={colors.stage}
  contrastNotes={{
    purpleAccessible: 'AA on bg.primary -- ratio ~4.5:1',
  }}
/>

---

## Status Colors

<SwatchGroup title="status" tokens={colors.status} />

---

## Spotify

<SwatchGroup title="spotify" tokens={colors.spotify} />

---

## Wrap Poster Gradients

<SwatchGroup title="wrapPoster" tokens={colors.wrapPoster} />

---

## Glow Effects

These are `box-shadow` values, not simple colors.

export function GlowSwatch({ name, value }) {
  return (
    <div style={{ display: 'flex', alignItems: 'center', gap: 16, marginBottom: 16 }}>
      <div
        style={{
          width: 64,
          height: 64,
          borderRadius: 12,
          backgroundColor: 'rgba(20, 20, 38, 0.65)',
          boxShadow: value,
          border: '1px solid rgba(255,255,255,0.06)',
          flexShrink: 0,
        }}
      />
      <div style={{ fontFamily: 'monospace', fontSize: 13 }}>
        <div style={{ color: '#eaeaf2', fontWeight: 600 }}>{name}</div>
        <div style={{ color: '#9999bb', fontSize: 11 }}>{value}</div>
      </div>
    </div>
  );
}

{Object.entries(colors.glow).map(([key, value]) => (
  <GlowSwatch key={key} name={key} value={value} />
))}

---

## Overlay Scale

White overlays at increasing opacity, used for elevation and hover states.

export function OpacitySwatch({ name, value }) {
  return (
    <div style={{ display: 'flex', alignItems: 'center', gap: 12, marginBottom: 6 }}>
      <div
        style={{
          width: 48,
          height: 48,
          borderRadius: 8,
          backgroundColor: value,
          border: '1px solid rgba(255,255,255,0.06)',
          flexShrink: 0,
          backgroundImage: 'linear-gradient(45deg, #222 25%, transparent 25%, transparent 75%, #222 75%), linear-gradient(45deg, #222 25%, transparent 25%, transparent 75%, #222 75%)',
          backgroundSize: '8px 8px',
          backgroundPosition: '0 0, 4px 4px',
        }}
      >
        <div style={{ width: '100%', height: '100%', borderRadius: 8, backgroundColor: value }} />
      </div>
      <div style={{ fontFamily: 'monospace', fontSize: 13 }}>
        <div style={{ color: '#eaeaf2', fontWeight: 600 }}>overlay.{name}</div>
        <div style={{ color: '#9999bb' }}>{value}</div>
      </div>
    </div>
  );
}

{Object.entries(colors.overlay).map(([key, value]) => (
  <OpacitySwatch key={key} name={key} value={value} />
))}

---

## Shade Scale

Black shades at increasing opacity, used for shadows and dimming.

{Object.entries(colors.shade).map(([key, value]) => (
  <OpacitySwatch key={key} name={`shade.${key}`} value={value} />
))}

---

## Aqua Alpha Scale

{Object.entries(colors.aquaAlpha).map(([key, value]) => (
  <OpacitySwatch key={key} name={`aquaAlpha.${key}`} value={value} />
))}

---

## Amber Alpha Scale

{Object.entries(colors.amberAlpha).map(([key, value]) => (
  <OpacitySwatch key={key} name={`amberAlpha.${key}`} value={value} />
))}
